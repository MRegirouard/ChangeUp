#if !defined ANGLECONVERSIONS_H
#define ANGLECONVERSIONS_H

#include "math.h"

const double toDeg = 180 / M_PI; // Multiply by radians to convert to degrees
const double toRad = M_PI / 180; // Multiply by degrees to convert to radians

#endif // ANGLECONVERSIONS_H